<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.dukusho_nv">
    <!-- <uses-permission -->
    <!-- android:name="android.permission.GET_ACCOUNTS" -->
    <!-- android:maxSdkVersion="22" /> -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar.FullScreen">




        <activity android:name=".view.BookSharedActivity"
            android:theme="@style/AppTheme"
            ></activity>
        <activity android:name=".view.PreShareActivity"
            android:theme="@style/AppTheme"
            />

        <activity android:name=".view.AddRepoActivity"
            android:theme="@style/AppTheme"
            />

        <activity
            android:name=".view.MyBooksActivity"
            android:label="@string/title_activity_my_books_actvity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.RepoActivity"
            android:label="@string/title_activity_repo"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LogInActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".view.books.BookPageActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".view.books.BookMainActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".view.books.GoToBookPageActivity"
            android:screenOrientation="landscape"
            />
    </application>

</manifest>